{
  "name": "fittelega-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^4.18.2",
    "openai": "^5.7.0",
    "react-router-dom": "^7.6.2",
    "telegraf": "^4.16.3"
  }
}
